schema_version = "lg.skill.v1"
id = "style-controller"
legacy_id = "style_controller"
version = "1.0.0"
entrypoint = "SKILL.md"
inputs = ["draft", "style_guide", "target_reader", "genre"]
outputs = ["style_diagnosis", "rewrite_guidelines", "sample_revision", "style_memory_updates"]
tools = ["memory_read", "write_output", "write_report"]
quality_checks = ["voice consistent", "dialogue ratio intentional", "sentence rhythm varied", "payoff density matches mode", "viewpoint stable"]
failure_modes = ["purple prose", "flat exposition", "voice drift", "dialogue explains too much"]
