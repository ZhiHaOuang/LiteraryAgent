schema_version = "lg.skill.v1"
id = "memory-updater"
legacy_id = "memory_updater"
version = "1.0.0"
entrypoint = "SKILL.md"
inputs = ["new_facts", "existing_memory", "artifact_refs"]
outputs = ["memory_diff", "files_to_update", "conflict_notes"]
tools = ["memory_read", "memory_update", "write_output"]
quality_checks = ["durable facts separated", "conflicts logged", "files named", "no silent overwrite", "timeline updated when needed"]
failure_modes = ["memory spam", "overwriting facts", "missing timeline change", "mixing brainstorm with canon"]
