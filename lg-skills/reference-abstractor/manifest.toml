schema_version = "lg.skill.v1"
id = "reference-abstractor"
legacy_id = "reference_abstractor"
version = "1.0.0"
entrypoint = "SKILL.md"
inputs = ["source_refs", "retrieved_patterns", "task_goal"]
outputs = ["abstract_mechanics", "applicable_constraints", "do_not_copy_notes"]
tools = ["reference_abstract", "write_output"]
quality_checks = ["no copied prose", "mechanic is reusable", "limits stated", "fit to current task explained"]
failure_modes = ["thin paraphrase", "retaining names or scenes", "mechanic too generic", "missing applicability"]
