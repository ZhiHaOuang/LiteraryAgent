schema_version = "lg.skill.v1"
id = "reference-retriever"
legacy_id = "reference_retriever"
version = "1.0.0"
entrypoint = "SKILL.md"
inputs = ["query", "reference_roots", "memory", "limits"]
outputs = ["source_refs", "candidate_patterns", "relevance_notes"]
tools = ["read_file", "search_file", "reference_search"]
quality_checks = ["source ids present", "query matched", "patterns separable from source wording", "context budget bounded"]
failure_modes = ["copying source prose", "no source ids", "too many irrelevant references", "unbounded context"]
