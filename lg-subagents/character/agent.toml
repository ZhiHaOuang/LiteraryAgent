schema_version = "lg.subagent.v1"
id = "character"
name = "CharacterAgent"
version = "1.0.0"
role = "负责人物弧光、关系网、动机、成长、创伤、误会、反转。"
model_profile = "writer"
prompt = "prompt.md"
output_schema = "output.schema.json"
tools = ["memory_read", "reference_search", "write_output"]
skills = ["character-arc-designer", "conflict-engine"]
inputs = ["premise", "world_rules", "plot_pressure"]
outputs = ["character_table", "relationship_map", "arc_plan"]
